const MD_CONFIG = {
  html: true,
  breaks: false,
  linkify: true,
};

const EMBED_YOUTUBE_CONFIG = {
  lazy: true,
};

const GRAY_MATTER_CONFIG = {
  excerpt: true,
  excerpt_separator: "<!-- excerpt -->",
};

const EMOJI_READ_CONFIG = {
  emoji: "(⌒▽⌒)☆:",
};

const DDMMYYYY = "dd.MM.yyyy";

module.exports = {
  MD_CONFIG,
  EMBED_YOUTUBE_CONFIG,
  GRAY_MATTER_CONFIG,
  EMOJI_READ_CONFIG,
  DDMMYYYY,
};
